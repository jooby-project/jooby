4.x

- review onComplete, the relationship with thread-locals variables. Where onComplete must run?? event-loop? separated thread? It depends on task
- review save session, similar to previous point. It depends on the kind of session where must run
- review requestCompleted, similar to previous point
