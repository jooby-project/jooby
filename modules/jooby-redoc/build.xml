<?xml version="1.0"?>
<project default="redoc">

  <target name="redoc">
    <property name="buildDir" value="${basedir}${file.separator}target${file.separator}classes" />
    <property name="redocDir" value="${buildDir}${file.separator}redoc" />
    <move file="${buildDir}${file.separator}META-INF${file.separator}resources${file.separator}webjars${file.separator}redoc${file.separator}${redoc.version}${file.separator}bundles" tofile="${redocDir}"/>
<!--    <replace file="${redocDir}/index.html" token="./" value="${swaggerPath}/" />-->
<!--    <replace file="${redocDir}/index.html" token="https://petstore.swagger.io/v2/swagger.json" value="${openAPIPath}" />-->
<!--    <delete>-->
<!--      <fileset dir="${redocDir}" includes="*.gz"/>-->
<!--    </delete>-->
  </target>

</project>
