<?xml version="1.0"?>
<project default="redoc">

  <target name="redoc">
    <property name="output" value="${basedir}${file.separator}target${file.separator}classes${file.separator}redoc" />
    <mkdir dir="${output}" />
    <copy file="node_modules${file.separator}redoc${file.separator}bundles${file.separator}redoc.standalone.js" tofile="${output}${file.separator}redoc.standalone.js"/>
  </target>

</project>
